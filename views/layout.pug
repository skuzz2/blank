doctype html
html
    head
        block vars
        title #{title}
        link(href = '/style.css', rel='stylesheet')
    body
        header
        div(id='content', class='item')
            h1 #{title}
            h3 #{description}
            block content                
        footer